# API Keys
# Get a new API key from https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=production
FLASK_DEBUG=0

# Database Configuration
# For production, you might want to use a different database
# DATABASE_URL=postgresql://username:password@hostname/database

# MindfulWealth API Configuration

# Database settings
DB_PATH=mindfulwealth.db

# API settings
PORT=5000
HOST=0.0.0.0

# JWT settings
# Generate a strong random key for production
# You can use: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=replace_with_a_strong_random_key_in_production

# CORS settings
# Set to your frontend domain in production
CORS_ORIGINS=https://your-production-domain.com 